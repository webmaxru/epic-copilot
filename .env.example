# Epic Copilot — Environment Variables

# Server port (default: 3000)
PORT=3000

# Azure Application Insights connection string (optional — enables telemetry)
# APPLICATIONINSIGHTS_CONNECTION_STRING=InstrumentationKey=...

# Azure DevOps Personal Access Token (future MCP server auth)
# ADO_MCP_AUTH_TOKEN=your-pat-token
